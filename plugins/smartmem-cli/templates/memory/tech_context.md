# Tech context — CLI tool

## Stack
- Language:
- Arg parser:
- Distribution:

## Commands
- Build: `(...)`
- Test: `(...)`
- Install local: `(...)`
- Smoke: `(my-cli --help)`

## Targets
- (which OSes / shells we support)
