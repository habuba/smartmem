# Tech context — software library

## Stack
- Language:
- Build tool:
- Test runner:
- Lint:
- Package registry:

## Commands
- Install: `(...)`
- Build: `(...)`
- Test: `(...) --watch` for dev
- Test (full): `(...)`
- Lint: `(...)`
- Release: `(...)`

## Pinned versions
- (note here)
