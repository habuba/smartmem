# Frontend

@../memory/frontend_context.md
@../memory/api_context.md

## Rules
- Use the project's chosen framework patterns. See `memory/frontend_context.md`.
- All API calls go through the typed client in `src/api/`. Never call fetch() directly from components.
- For UI changes, run the dev server and exercise the feature in a browser before reporting done.
